/**
 * User-visible app name in chrome (e.g. top bar). Replaced when scaffolding: TestBareBootStrapWorkflow → MyApp.
 * Keep aligned with `app.json` `displayName` / native display name.
 */
export const APP_DISPLAY_NAME = 'TestBareBootStrapWorkflow';
